export const metadata = {
  title: 'Based Goose Network 🦆',
  description: 'Welcome to Based Goose Money — powered by BONK.FUN on the SOL Network.',
};


export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
